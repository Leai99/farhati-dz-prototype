/**
 * Mock "signed-in provider" for the Service Provider App session.
 *
 * providers[0] (p1) only has one active service, so p2 (استوديو لمسة
 * للتصوير) is used instead for a richer, more representative demo — see
 * ProviderDashboard's original note. Shared here so Dashboard, Services,
 * Requests and Profile all agree on the same "logged-in" provider.
 */
export const CURRENT_PROVIDER_ID = 'p2'
